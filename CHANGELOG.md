# Changelog

这里记录 MoonTrace 从 v0.2.0 之后比较值得记的改动。

## v0.3.0-beta.2（Windows 便携测试版）

- Windows 打包后增加视窗后端导入验收，补上此前遗漏的 pythonnet 检查。
- 遇到 Python.Runtime.Loader.Initialize 错误时，自动使用 Edge 独立应用窗口。
- 便携数据保存在 EXE 旁的 MoonTraceData；暂用默认图标。

## v0.3.0-beta.1（预发布测试版）

- 桌面入口：在 Windows 独立窗口中运行已有 Web 下载服务。
- SQLite 任务中心支持下载队列、持久化历史、失败重试；Web 与 Telegram 共用。
- Web 变成首页、下载、设置三个页面；增加可视化主题设置与本地背景上传。
- 打包后私人配置、主题和下载历史保存到 `%LOCALAPPDATA%\MoonTrace`。
- 提供 Windows 打包脚本；正式安装包仍需在 Windows 上构建及验证。

## v0.2.13

- Web 与 Telegram 的视频画质选择现在会传递 Bilibili `quality_id`，可区分相同分辨率下的 1080P、1080P+ 等不同画质档位。
- 修正 Bilibili 封面下载逻辑。
- 不再写死固定分辨率，也不再优先使用 `cover43` 这类 4:3 展示封面。
- 现在优先读取普通投稿封面的 `pic`，并去掉 CDN 的 `@...` 缩放 / 裁剪参数后下载。
- 按钮与文件名不再标注固定尺寸，尽量保留 B 站实际提供的封面尺寸。

## v0.2.12

- `setup.bat` 现在会检查 FFmpeg；电脑里没有时，会自动下载到项目目录。
- 下载器会优先使用 MoonTrace 自己的 FFmpeg，不再要求用户手动配置环境变量。
- 启动和下载时补了 FFmpeg 检查，缺东西时会直接提示重新运行安装脚本。

## v0.2.11

- 把更新日志重新写了一遍，少一点“发布说明”的味道。
- 整理 GitHub 首次公开发布要用的文件。

## v0.2.10

- 网页里加了更新日志。
- 补了版权和使用边界说明。
- 仓库里加上 `CHANGELOG.md`、`LEGAL.md` 和 `LICENSE`。

## v0.2.9

- 加了一个使用方法。
- 特别写了 Bilibili 高画质需要登录这件事。
- 首页标语换成了 `Follow the trace beyond the moon.`

## v0.2.8

- 给网页加了点型月味的装饰。
- 月亮、细线、边角这些。
- 没加重动画，怕又卡。

## v0.2.7

- 修了背景图不显示。
- 原因是 `z-index` 写错了，背景被压到页面后面去了。

## v0.2.6

- 上一版滚动太卡，把大面积毛玻璃砍了。
- 背景换成原 PNG，不再压成 JPG。

## v0.2.5

- 第一次认真改网页外观。
- 加了背景、半透明卡片和蓝紫色主题。

## v0.2.4

- 修复竖屏视频显示成 `1772P` 这种奇怪画质的问题。
- 弹幕和字幕分开了。
- 现在可以单独下载弹幕 XML。

## v0.2.3

- Telegram 发完文件后，可以选择要不要保留电脑里的副本。
- 删除前加了二次确认，避免误删。

## v0.2.2

- Windows 的 BAT 之前被编码坑了一次。
- 改成 ASCII + CRLF，并直接调用项目里的 Python。

## v0.2.1

- 继续修启动脚本。
- 主要是让 Windows 下一键启动靠谱一点。

## v0.2.0

- 项目正式改名 MoonTrace。
- Web 和 Telegram Bot 开始共用同一套核心和设置。
- 为以后接 YouTube、TikTok 留了平台层。
